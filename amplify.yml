version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "No build commands needed for static site"
    build:
      commands:
        - echo "Building frontend..."
  artifacts:
    baseDirectory: frontend
    files:
      - '**/*'
  cache:
    paths: []
